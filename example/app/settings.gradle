throw new GradleException("""
Do not open the 'app' folder directly in Android Studio.

Open the parent 'example' folder instead:
  File → Open → select .../example/

Then pick the 'app' run configuration.

See ../OPEN_IN_ANDROID_STUDIO.txt for details.
""".stripIndent())
